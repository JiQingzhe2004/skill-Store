# Skill Store 开发任务清单

> 说明：
>
> - `P0`：必须优先完成，直接影响主链路。
> - `P1`：v1 重要能力，建议在主链路完成后立即补齐。
> - `P2`：可延后，不阻塞 v1 上线。
> - 任务状态默认未开始，可按 `[ ]`、`[~]`、`[x]` 标记。
> - 本次勾选基于当前仓库已落地代码与文档，不包含尚未跑通的构建、迁移、联调结果。

---

## 0. 项目前置确认

### 0.1 产品规则确认

- [ ] `P0` 确认 Skill 的 `content` 格式：纯文本、JSON，还是双格式支持。
- [ ] `P0` 确认 `manifest` 的字段协议和最小必填项。
- [ ] `P0` 确认 `unlisted` 是否允许被第三方 API 访问。
- [ ] `P1` 确认平台内安装后是否默认跟随最新版本。
- [ ] `P1` 确认公开发布是否需要管理员审核。

验收标准：

- 以上规则形成明确文档，后续数据库字段和接口行为不再反复修改。

---

## 1. 项目骨架

### 1.1 仓库结构初始化

- [x] `P0` 规划目录结构：`apps/web`、`apps/api`、`packages/shared`。
- [x] `P0` 初始化前端项目。
- [x] `P0` 初始化后端项目。
- [x] `P0` 配置根目录包管理和基础脚本。
- [x] `P1` 建立共享类型或公共常量目录。

### 1.2 基础工程配置

- [x] `P0` 配置环境变量文件模板：前端、后端、数据库、SMTP。
- [ ] `P0` 配置统一的代码风格方案。
- [x] `P0` 配置基础错误处理和统一响应结构。
- [ ] `P1` 配置接口前缀和版本路由规则。
- [ ] `P1` 配置基础日志方案。

### 1.3 数据库基础能力

- [x] `P0` 接入 `Prisma`。
- [x] `P0` 配置 `MySQL` 连接。
- [x] `P0` 建立首个 migration 流程。
- [ ] `P1` 准备本地初始化数据方案。

验收标准：

- 前后端项目均可独立启动开发环境。
- 数据库迁移可执行。
- 环境变量、公共响应格式、日志基础能力已建立。

---

## 2. 用户认证系统

### 2.1 数据模型

- [x] `P0` 建立 `users` 表。
- [x] `P0` 建立 `email_codes` 表。
- [x] `P1` 为用户角色预留 `role` 字段与枚举。

### 2.2 后端认证接口

- [x] `P0` 实现注册接口。
- [x] `P0` 实现邮箱验证码发送接口。
- [x] `P0` 实现邮箱验证接口。
- [x] `P0` 实现登录接口。
- [x] `P0` 实现忘记密码接口。
- [x] `P0` 实现重置密码接口。
- [x] `P0` 实现 JWT 鉴权中间件或守卫。
- [x] `P1` 实现当前登录用户信息接口。

### 2.3 安全控制

- [x] `P0` 密码哈希存储。
- [x] `P0` 注册、登录、验证码发送加限流。
- [x] `P1` 验证码设置过期时间和使用状态。
- [ ] `P1` 登录失败记录日志。

### 2.4 前端页面

- [x] `P0` 完成注册页。
- [x] `P0` 完成登录页。
- [x] `P0` 完成邮箱验证页。
- [x] `P0` 完成忘记密码页。
- [x] `P0` 完成重置密码页。
- [x] `P1` 完成登录态持久化与路由保护。

验收标准：

- 用户可完成注册、验证邮箱、登录、找回密码全流程。
- 未登录用户无法访问需要鉴权的页面和接口。

---

## 3. 技能数据模型与核心领域能力

### 3.1 技能数据表

- [ ] `P0` 建立 `skills` 表。
- [ ] `P0` 建立 `skill_versions` 表。
- [ ] `P0` 建立 `user_installed_skills` 表。
- [ ] `P1` 建立 `skill_usage_logs` 表。

### 3.2 技能基础规则

- [ ] `P0` 实现 `slug` 唯一校验。
- [ ] `P0` 实现 `visibility` 枚举校验。
- [ ] `P0` 实现 `status` 枚举校验。
- [ ] `P0` 实现版本号语义化校验。
- [ ] `P1` 约束已发布版本不可覆盖修改。

### 3.3 Skill 结构校验

- [ ] `P0` 定义 skill 输入 DTO。
- [ ] `P0` 定义 `schema` 校验规则。
- [ ] `P0` 定义 `manifest` 校验规则。
- [ ] `P1` 对公开发布前进行完整性校验。

验收标准：

- 技能领域模型稳定。
- 数据库能承载草稿、发布版本、安装记录三类核心数据。

---

## 4. 技能管理后台接口

### 4.1 技能 CRUD

- [ ] `P0` 实现创建技能接口。
- [ ] `P0` 实现我的技能列表接口。
- [ ] `P0` 实现技能详情接口。
- [ ] `P0` 实现编辑技能接口。
- [ ] `P0` 实现删除技能接口。

### 4.2 版本发布

- [ ] `P0` 实现保存草稿逻辑。
- [ ] `P0` 实现发布版本接口。
- [ ] `P0` 实现技能版本列表接口。
- [ ] `P1` 实现指定版本详情接口。
- [ ] `P1` 发布时同步更新 `latest_version`。

### 4.3 权限控制

- [ ] `P0` 限制用户只能修改自己的技能。
- [ ] `P0` 非公开技能禁止游客访问。
- [ ] `P1` 为管理员预留下架和归档逻辑。

验收标准：

- 登录用户可独立完成技能创建、编辑、保存、发布。
- 不同用户之间的数据隔离正确。

---

## 5. 技能编辑器与控制台页面

### 5.1 我的技能页

- [ ] `P0` 完成我的技能列表页。
- [ ] `P0` 支持按状态筛选。
- [ ] `P1` 支持按名称或标签搜索。

### 5.2 新建/编辑技能页

- [ ] `P0` 完成基础表单：名称、slug、描述、标签。
- [ ] `P0` 完成可见性与状态设置。
- [ ] `P0` 完成 `content` 编辑区域。
- [ ] `P0` 完成 `schema` 编辑区域。
- [ ] `P0` 完成 `manifest` 编辑区域。
- [ ] `P0` 支持保存草稿。
- [ ] `P0` 支持发布版本。
- [ ] `P1` 支持发布前预校验提示。

### 5.3 版本展示

- [ ] `P1` 完成版本历史列表。
- [ ] `P1` 展示每个版本的 `changelog`。
- [ ] `P2` 支持查看历史版本内容。

验收标准：

- 用户可通过页面完整管理一个技能生命周期。
- 发布前后状态变化在 UI 中清晰可见。

---

## 6. 公开市场

### 6.1 公开技能列表

- [ ] `P0` 实现公开技能列表接口。
- [ ] `P0` 完成公开技能市场页。
- [ ] `P0` 支持分页。
- [ ] `P1` 支持关键词搜索。
- [ ] `P1` 支持标签筛选。

### 6.2 技能详情页

- [ ] `P0` 实现公开技能详情接口。
- [ ] `P0` 完成技能详情页。
- [ ] `P0` 展示版本信息。
- [ ] `P1` 展示作者信息和最近更新时间。

### 6.3 平台内安装

- [ ] `P0` 实现安装接口。
- [ ] `P0` 写入 `user_installed_skills`。
- [ ] `P0` 在详情页提供安装按钮。
- [ ] `P1` 完成我的安装记录页。

验收标准：

- 游客可浏览公开技能。
- 登录用户可安装公开技能并看到安装记录。

---

## 7. API Key 与第三方集成

### 7.1 API Key 管理

- [ ] `P0` 建立 `api_clients` 表。
- [ ] `P0` 实现创建 API Key 接口。
- [ ] `P0` 实现 API Key 列表接口。
- [ ] `P0` 实现禁用 API Key 接口。
- [ ] `P1` 实现删除 API Key 接口。
- [ ] `P0` API Key 仅明文展示一次。
- [ ] `P0` 数据库存储哈希值。

### 7.2 对外只读 API

- [ ] `P0` 实现 `GET /api/v1/skills`。
- [ ] `P0` 实现 `GET /api/v1/skills/:slug`。
- [ ] `P0` 实现 `GET /api/v1/skills/:slug/manifest`。
- [ ] `P0` 接入 `x-api-key` 鉴权。
- [ ] `P0` 限制只返回允许对外访问的数据。

### 7.3 日志和限流

- [ ] `P1` 为第三方接口加限流。
- [ ] `P1` 记录 API 调用日志。
- [ ] `P2` 增加基础调用统计。

### 7.4 控制台页面

- [ ] `P0` 完成 API Key 管理页。
- [ ] `P1` 提供接口调用示例和文档入口。

验收标准：

- 用户可创建并管理 API Key。
- 第三方应用可通过 API Key 拉取技能详情和 manifest。

---

## 8. 管理后台

### 8.1 用户管理

- [ ] `P2` 用户列表接口。
- [ ] `P2` 用户列表页面。

### 8.2 技能管理

- [ ] `P1` 公开技能下架接口。
- [ ] `P2` 技能审核列表页。
- [ ] `P2` 技能下架记录展示。

### 8.3 日志查看

- [ ] `P2` 基础操作日志接口。
- [ ] `P2` 基础日志页。

验收标准：

- 管理员具备最基础的平台治理能力。

---

## 9. 安全、稳定性与质量

### 9.1 安全

- [x] `P0` 统一输入校验。
- [ ] `P0` 统一权限校验。
- [x] `P0` 响应中避免泄露敏感字段。
- [x] `P1` 增加 CORS 安全配置。
- [ ] `P1` 增加常见安全头配置。

### 9.2 错误处理与日志

- [x] `P0` 统一异常处理器。
- [ ] `P0` 关键失败日志落库或落文件。
- [x] `P1` 区分业务错误与系统错误。

### 9.3 测试

- [ ] `P0` 为认证流程补单元/集成测试。
- [ ] `P0` 为技能发布流程补测试。
- [ ] `P0` 为 manifest 输出补测试。
- [ ] `P1` 为关键前端表单补测试。

### 9.4 文档

- [x] `P0` 编写项目启动说明。
- [x] `P0` 编写环境变量说明。
- [ ] `P0` 编写 API 文档。
- [ ] `P1` 编写 Skill 格式说明文档。

验收标准：

- 核心链路有基本测试覆盖。
- 新开发者可以根据文档完成本地启动。

---

## 10. 推荐执行顺序

建议严格按下面顺序推进：

1. 完成项目前置确认。
2. 完成项目骨架和数据库接入。
3. 完成用户认证系统。
4. 完成技能数据模型和后台接口。
5. 完成技能编辑器和控制台页面。
6. 完成公开市场和平台内安装。
7. 完成 API Key 与第三方集成。
8. 最后补后台治理、安全、测试和文档。

---

## 11. v1 最小上线清单

以下任务全部完成，即可认为达到 v1 最低上线条件：

- [ ] 用户注册、邮箱验证、登录、找回密码。
- [ ] 技能创建、编辑、删除、保存草稿、发布版本。
- [ ] 技能公开范围控制。
- [ ] 公开技能列表页、详情页、搜索。
- [ ] 平台内安装能力。
- [ ] API Key 管理。
- [ ] 第三方读取技能详情和 manifest。
- [ ] 基础限流、日志、错误处理。
- [ ] 核心链路测试。
- [ ] 启动文档和 API 文档。

---

## 12. 当前建议先做的 10 个任务

如果现在立刻开工，先做这 10 个：

1. [ ] 明确 `content` 和 `manifest` 协议。
2. [x] 初始化后端项目和 `Prisma`。
3. [ ] 建立 `users`、`email_codes`、`skills`、`skill_versions` 表。
4. [x] 实现注册、验证邮箱、登录接口。
5. [x] 初始化前端项目和路由。
6. [x] 完成登录页、注册页、验证页。
7. [ ] 实现技能创建、编辑、我的技能列表接口。
8. [ ] 完成新建/编辑技能页。
9. [ ] 实现发布版本接口和公开技能列表接口。
10. [ ] 完成技能详情页和安装接口。
